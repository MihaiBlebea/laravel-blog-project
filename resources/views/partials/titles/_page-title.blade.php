<div class="mt-3 mb-5 text-center">
    <h1 class="mb-2 text-capitalize">{{ $title }}</h1>

    @if(isset($subtitle))

        <h4 class="text-muted text-lowercase">{{ $subtitle }}</h4>

    @endif
</div>
